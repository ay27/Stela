# Product screenshot provenance

All product PNGs are actual UI captures from 2026-09-13. Stela captures use the
marketing worktree and an isolated profile with the public Demo Vault.
WebP files are compressed derivatives; -800 files are resized for smaller screens.

| Feature | File | Visible state |
| --- | --- | --- |
| SQL in Markdown | markdown.png | SQL block and saved public example result table |
| SQL in Chat | chat.png | Actual composer with SQL and natural language; unsent draft |
| Data Agent | agent.png | Agent Panel beside the current Canvas, ready for a task |
| Canvas | canvas.png | Saved public example charts and table |
| Knowledge | knowledge.png | Knowledge manager with a bundled active example skill |
| Database plugins | plugins.png | Public MySQL connector installed and loaded |
| Local-first | local.png | Finder showing Markdown, Chat and Canvas files in the isolated Vault |

Stela screenshots are 1199 × 768. The Finder capture is 920 × 464, with its personal
sidebar hidden during capture and restored afterwards. No personal Vault files or
model credentials appear in these images.

Chat and Agent images do not show a newly completed model run. The knowledge entry
is bundled demo content, not a new automatic-maintenance outcome. Captions make the
relevant state explicit. Both website locales use these real captures.

Earlier live-capture limits remain: model-configuration authorization was not
received, and starting the local MySQL demo container was rejected by automatic
approval review. This screenshot update did not retry those blocked actions.

Additional workspace/connections captures remain available as supporting material.

## Website selection preview — 2026-09-17

The hero and all six feature rows now reuse the real captures listed above.
The hero uses workspace.png; Local-first is not included. This is an initial
image selection preview, not a new capture session. Each image links to its
full-size PNG. Chat remains an unsent draft and Agent remains ready for a task.

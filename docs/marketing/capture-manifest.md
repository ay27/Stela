# Website capture provenance

Real user-supplied Demo Vault captures, September 21–22, 2026.

The website uses 16 composed images under `docs/assets/screenshots`. Each includes an HTML/CSS heading and a proportionally scaled, complete real capture. No SQL, data, or app text was repainted. Source filenames, SHA-256 hashes, full-image bounds, and copy are recorded in `manifest.json`. Originals remain outside the repository.

- Every capture is shown in full, including toolbars, sidebars and input areas. No content is cropped or covered; unused space stays outside the screenshot.
- Chinese knowledge is the preloaded ecommerce-unit-economics Skill.
- English knowledge shows maintained ecommerce-marketing-spend-grain with source excerpts.
- Chinese Agent uses the complete replacement capture from September 22.
- English Agent retains the workspace update capture. The replacement en-chat image instead shows report analysis and includes an incorrect 60% spend claim (the figures imply 75%); it is pending correction.
- SQL Chat uses zh-sql-in-chat.png and en-sql-in-chat.png supplied September 22. Both are shown in full, including the result table, SQL editor and completion popup. Content already outside the app viewport in the original capture is unchanged.
- Lineage uses the expanded `lineage2` captures with actual amounts.

PNG files open at full size; WebP images provide 900px and 1800px variants.

Hero compositions use the complete zh-agent.png and en-agent.png captures, with the file tree visible. The overview heading is retained. The English source is displayed unchanged, including its previously noted spend-share discrepancy.

The Chinese and English Markdown captures were replaced with user-edited originals on September 22: the toolbar now shows only the demo-vault-relative path. Full-image composition is preserved.

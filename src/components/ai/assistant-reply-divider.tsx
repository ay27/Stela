/** Boundary between a user turn and all of its execution/reply content. */
export function AssistantReplyDivider() {
  return <hr className="stela-assistant-reply-divider w-full border-0 border-t border-border/60" />;
}

/**
 * callAgentTurn was removed in the pi-ai migration (ADR-0018).
 * Provider/agent behavior is covered by build + manual smoke.
 */
console.log("ai provider tests passed (callAgentTurn removed).");
